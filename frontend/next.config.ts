import type { NextConfig } from "next";

/** Next.js config boilerplate. Feature work per docs/TASKS.md Phase 8+. */
const nextConfig: NextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
